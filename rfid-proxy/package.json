{
  "name": "sudortime-rfid-proxy",
  "version": "1.0.0",
  "description": "HTTP receiver for RFID microcontrollers that forwards reads to the SudorTime API over HTTPS.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=18"
  }
}
